<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">#FFFFFF</item>
        <item name="colorAccent">@color/AzulLogin</item>
    </style>

    <!--colocando o de login -->
    <style name="AppThemeLogin" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/AzulLogin</item>
        <item name="colorPrimaryDark">@color/AzulLogin</item>
        <item name="colorAccent">#FFFFFF</item>
    </style>

</resources>
